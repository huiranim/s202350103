<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.choongang.s202350103.PointChargeMapper"> 

	<insert id="gbInsertPointCharge" parameterType="Orderr">
		INSERT INTO point_list(p_num, m_num, a_par_pdate, p_list_type, m_point)
		VALUES(pointlist_p_num_seq.nextval, #{m_num}, sysdate, 8, #{o_pay_price})
	</insert>
	
	<update id="gbUpdateMemberPoint" parameterType="Orderr">
		UPDATE member
		SET m_point = m_point + #{o_pay_price}
		WHERE m_num = #{m_num}
	</update>
</mapper>